Start by creating a simple agent that can answer questions and call tools. The agent will use DeepSeek as its language model, a basic weather function as a tool, and a simple prompt to guide its behavior.
